# Source of Truth — Document Lifecycle & Canonical Index

**Authority:** OmniQuest Media Inc. (OQMInc™) · Governed by ZoneGPT (MaxZone /
OQMIgpt) — Corpus v11

This file is the **override map** for documentation. A doc is authoritative
because this index says so — not because of its filename or its position in the
tree. When two docs disagree, the one named canonical here wins; the other is
Superseded and must carry a banner pointing here.

---

## 1. Document lifecycle states

| State          | Meaning                                                  | Action                                             |
| -------------- | -------------------------------------------------------- | -------------------------------------------------- |
| **CANONICAL**  | Current source of truth for its domain.                  | Keep. Update in place. Never archive.              |
| **SUPERSEDED** | Replaced by a canonical doc.                             | Banner → archive after ratification.               |
| **HISTORICAL** | Spent point-in-time report (status/summary/report-back). | Archive under the quarantine rule.                 |
| **UNKNOWN**    | Not yet triaged.                                         | Human decides: promote, mark superseded, or leave. |

Machine triage of every tracked doc into CANONICAL / HISTORICAL / UNKNOWN is
regenerated by `scripts/classify-docs.js` into `docs/DOC_CLASSIFICATION.md`.

---

## 2. Canonical index (curated)

The docs pinned CANONICAL below are also listed in `.github/required-files.txt`,
which `scripts/classify-docs.js` reads so a reports-scope archival pass never
sweeps them. Keep the two in lockstep: add or remove a canonical doc here
**and** in that file together. This index is a **CEO-ratifiable seed** from the
doc-kit install — extend or trim it as the corpus settles.

> **Governing charter — `RRR-GOV-002`.** Roles, modes, and the non-negotiable
> invariants (financial-integrity/FIZ, tenant isolation, PII & secrets, naming
> canon, governance, commit-prefix enum) are owned by the RedRoomRewards
> charter, **not** by this file. `CLAUDE.md` is a thin pointer to it. The
> charter text is preserved in-repo at
> `archive/governance/RRR-GOV-002_2026-04-21.md`; the day-to-day agent entry
> points are `.github/copilot-instructions.md` and `.github/AGENTS.md`. (Note:
> the charter's self-declared live path under
> `PROGRAM_CONTROL/DIRECTIVES/QUEUE/` was consolidated in the 2026-05-23
> governance archive; that pointer is stale — flagged for the CEO, out of scope
> for this doc-kit pass.)

| Domain                         | Canonical doc                                |
| ------------------------------ | -------------------------------------------- |
| AI-assistant guidance          | `CLAUDE.md` (pointer → `RRR-GOV-002`)        |
| Doc lifecycle & canonical map  | `docs/SOURCE_OF_TRUTH.md`                    |
| Repo overview                  | `README.md`                                  |
| Contributor workflow           | `CONTRIBUTING.md`                            |
| Security policy                | `SECURITY.md`                                |
| Domain vocabulary              | `docs/DOMAIN_GLOSSARY.md`                    |
| Loyalty engine spec            | `docs/RRR_LOYALTY_ENGINE_SPEC_v1.1.md`       |
| CEO decisions of record        | `docs/RRR_CEO_DECISIONS_FINAL_2026-04-17.md` |
| Requirements master            | `docs/REQUIREMENTS_MASTER.md`                |
| Promotion rules                | `docs/PROMOTION_RULES.md`                    |
| Feature compliance             | `docs/FEATURE_COMPLIANCE_CHECKLIST.md`       |
| Architecture                   | `docs/ARCHITECTURE.md`                       |
| API surface                    | `docs/API_SURFACE.md`                        |
| Auth contract                  | `docs/AUTH_CONTRACT.md`                      |
| Database schema                | `docs/DATABASE_SCHEMA.md`                    |
| Event architecture             | `docs/EVENT_ARCHITECTURE.md`                 |
| Integration contract           | `docs/INTEGRATION_CONTRACT.md`               |
| Wallet & escrow architecture   | `docs/WALLET_ESCROW_ARCHITECTURE.md`         |
| Queue integration              | `docs/QUEUE_INTEGRATION_GUIDE.md`            |
| Operational runbook            | `docs/OPERATIONAL_RUNBOOK.md`                |
| Disaster-recovery runbook      | `docs/DISASTER_RECOVERY_RUNBOOK.md`          |
| PIPEDA breach procedure        | `docs/PIPEDA_BREACH_PROCEDURE.md`            |
| Status-page incident templates | `docs/STATUS_PAGE_TEMPLATES.md`              |
| Production deploy spec         | `docs/PRODUCTION_DEPLOY_SPEC.md`             |
| Staging deploy spec            | `docs/STAGING_DEPLOY_SPEC.md`                |
| Keycloak realm spec            | `docs/KEYCLOAK_REALM_SPEC.md`                |

Everything not pinned here or listed under HISTORICAL in
`docs/DOC_CLASSIFICATION.md` is **UNKNOWN** — left alone pending human
ratification, not an archival candidate.

---

## 3. Quarantine & burn policy

Archives are a **quarantine, not a graveyard**. Git history keeps every version,
so quarantine is reversible and burning is cheap.

1. **Archive, don't delete.** `git mv` a SUPERSEDED/HISTORICAL doc into
   `archive/<YYYY-MM>/…` preserving its path, with a banner:
   `> ARCHIVED <date> — superseded by <canonical> / spent report. Not for current work.`
2. **Tripwire.** `archive/MANIFEST.md` logs each quarantined doc with a
   `consulted?` column. If anyone needs an archived doc, they log it and promote
   that fact into the live canonical doc.
3. **Burn on green launch.** Reach a ship milestone with an empty `consulted?`
   log → the archive is provably dead → delete it in one `CHORE:` commit.

---

## 4. Governance guardrails (do not fast-path these)

- **Governance/doctrine docs are Human-Review Category** — archiving or editing
  the charter, `.github/copilot-instructions.md`, `.github/AGENTS.md`, or any
  `docs/governance/` doc requires CEO merge. Reports-scope archival (spent
  HISTORICAL reports at root + `docs/`) is the only fast-path.
- **`.github/required-files.txt` pins CANONICAL docs** —
  `scripts/classify-docs.js` reads it so pinned docs are never archival
  candidates. It is not yet wired to a structure-validation CI gate in this
  repo; if one is added later, moving a pinned doc must update this list in
  lockstep.
- **Restricted-path gate** — the archival pass skips any doc whose path contains
  `ledger`/`consent`/`pii` (fail-closed), mirroring the CI restricted-path
  detection in `.github/workflows/ci.yml` (`src/(ledger|wallets|consent)/`,
  `pii`). Those code paths are never touched by a docs pass.

---

## 5. Current scope

Classification + indexing only. Archival is **reports scope** (the HISTORICAL
bucket). SUPERSEDED working docs and governance retirement are deferred to
ratified, CEO-gated follow-ups.
