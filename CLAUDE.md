# CLAUDE.md

> **Thin pointer — not a source of truth.** This file exists only so that Claude
> Code (and other agents that look for a root `CLAUDE.md`) are redirected to the
> repository's real governing charter. It deliberately carries **no invariants,
> build state, or governance content of its own** — that content was
> consolidated into **RRR-GOV-002** and this file must not re-grow a competing
> copy of it (see _Why this is only a pointer_ below).

## Where the real guidance lives

**Governing charter: `RRR-GOV-002` — the persistent Architecture & Coding
Charter for RedRoomRewards.** It is the single source of truth for roles, modes,
invariants (financial-integrity / FIZ, tenant isolation, PII & secrets, naming
canon, governance), CEO decisions in force, and the active work stream. Do not
duplicate or paraphrase its rules here — read it directly and mirror it.

- Charter text (preserved copy in-repo):
  `archive/governance/RRR-GOV-002_2026-04-21.md`
- Agent operating instructions / entry point: `.github/copilot-instructions.md`
  and `.github/AGENTS.md`
- Domain vocabulary & commit-prefix authority: `docs/DOMAIN_GLOSSARY.md`
- Contributor workflow & gates: `CONTRIBUTING.md`
- Doc lifecycle & canonical index: `docs/SOURCE_OF_TRUTH.md` (machine triage in
  `docs/DOC_CLASSIFICATION.md`, regenerated by `scripts/classify-docs.js`)

If anything here ever conflicts with `RRR-GOV-002`, the charter wins.

<!-- BEGIN:generated:repo-facts -->
<!-- Auto-generated by scripts/refresh-claude-md.js. Do not edit by hand. -->

```text
Package manager        npm
Node engines           >=22.0.0
Tracked markdown docs  199
```

<!-- END:generated:repo-facts -->

---

## Why this is only a pointer

`RRR-GOV-002 §1` was authored precisely to end the multi-document drift that a
stale `CLAUDE.md` had caused, and `§8` lists _"Reintroducing CLAUDE.md"_ as
out-of-scope. The original `CLAUDE.md` was archived under charter task A-001.
Reintroducing a **thin pointer** — one that holds no rules of its own and simply
routes readers to the charter — reinforces `RRR-GOV-002` as the sole source of
truth rather than competing with it. Anyone tempted to "fill this in" with
invariants or build state should instead update `RRR-GOV-002`; this file stays a
signpost. Retiring even this pointer (deleting the file) is a legitimate CEO
call — the doc-freshness kit below functions with or without a `CLAUDE.md`.

---

## Keeping this file fresh

This file is split into **curated prose** (changed by hand) and a **generated
facts block** (never edited by hand). Two mechanisms fight rot:

- **`scripts/refresh-claude-md.js`** rewrites the
  `<!-- BEGIN:generated:repo-facts -->` block from live repo signals. It writes
  no timestamp, so it only diffs when a fact changed. A workflow
  (`.github/workflows/claude-md-refresh.yml`) runs it on push to the default
  branch and self-commits `[skip ci]`. Run it locally with
  `node scripts/refresh-claude-md.js`; `--check` exits non-zero if stale.
- **`scripts/check-claude-md.js`** runs in CI
  (`.github/workflows/claude-md-check.yml`) and fails if any repo path
  referenced in this file has stopped existing.

This kit is repo-agnostic and copied verbatim across OQMInc repositories.
